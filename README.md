# Jobs

## Run Dashboard
```bash
dotnet run --project ./src/Dashboard/Dashboard.csproj
````

## Run Node

To run multiple nodes, simply execute the command multiple times:

```bash
dotnet run --project ./src/Node/Node.csproj
```
